apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: firebase-auth-validator-ingress
  annotations:
    # Allow CORS for frontend apps
    nginx.ingress.kubernetes.io/enable-cors: "true"
    nginx.ingress.kubernetes.io/cors-allow-origin: "https://aggregator-demo.development.wapps.ai,https://aggregator.development.wapps.ai,https://discussion-csr.development.wapps.ai"
    nginx.ingress.kubernetes.io/cors-allow-methods: "GET, POST, OPTIONS"
    nginx.ingress.kubernetes.io/cors-allow-headers: "Content-Type, Authorization"
spec:
  rules:
  # Auth BFF endpoint for frontend apps
  - host: auth.development.wapps.ai
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: firebase-auth-validator
            port:
              number: 80

