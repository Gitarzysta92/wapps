apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: management-portal-csr-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: management-portal-csr.development.wapps.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: management-portal-csr-service
            port:
              number: 80

