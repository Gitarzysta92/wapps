name: 'Editorial Service - Build & Deploy'

on:
  push:
    branches: [develop, main]
    paths:
      - 'apps/services/editorial/**'
      - 'environments/dev/apps/editorial-kustomization/**'
      - '.github/workflows/editorial.workflow.yml'
      - '.github/actions/deploy-to-argocd/**'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        default: 'development'
        type: choice
        options:
          - development
          - production

env:
  SERVICE_NAME: editorial
  REGISTRY: ghcr.io
  IMAGE_NAME: gitarzysta92/wapps/editorial

jobs:
  build:
    name: 'Build Docker Image'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      environment: ${{ steps.set-env.outputs.environment }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set environment
        id: set-env
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            ENV="${{ github.event.inputs.environment }}"
          elif [ "${{ github.ref }}" == "refs/heads/main" ]; then
            ENV="development"
          else
            ENV="development"
          fi
          echo "environment=${ENV}" >> $GITHUB_OUTPUT
          echo "Environment: ${ENV}"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build application
        run: npx nx build apps.services.editorial --configuration=production

      - name: Build admin panel
        run: |
          echo "Building Strapi admin panel..."
          npx nx build-admin apps.services.editorial || {
            echo "❌ Build admin panel failed!"
            echo "Checking if strapi is available in node_modules:"
            ls -la node_modules/.bin/strapi || echo "strapi not found in root node_modules/.bin"
            echo "Checking apps/services/editorial/node_modules:"
            ls -la apps/services/editorial/node_modules/.bin/strapi 2>/dev/null || echo "strapi not found in local node_modules/.bin"
            exit 1
          }
      
      - name: Verify admin panel build
        run: |
          if [ ! -d "apps/services/editorial/dist/build" ]; then
            echo "❌ Admin panel build folder not found!"
            echo "Contents of apps/services/editorial:"
            ls -la apps/services/editorial/ || true
            echo "Checking if build folder exists elsewhere:"
            find . -type d -name "build" -path "*/editorial/*" 2>/dev/null || echo "No build folder found"
            exit 1
          else
            echo "✅ Admin panel build folder found"
            echo "Build folder size:"
            du -sh apps/services/editorial/dist/build
            echo "Build folder contents (first 10 items):"
            ls -la apps/services/editorial/dist/build/ | head -10
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: gitarzysta92
          password: ${{ secrets.CONTAINER_REGISTRY_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/services/editorial/Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          no-cache: true

      - name: Image pushed
        run: |
          echo "✅ Image built and pushed: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest"

  deploy:
    name: 'Deploy to ${{ needs.build.outputs.environment }}'
    runs-on: self-hosted
    needs: build
    environment: ${{ needs.build.outputs.environment }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set environment variables
        id: env-vars
        run: |
          ENV="${{ needs.build.outputs.environment }}"
          ENV_SHORT=$(echo "$ENV" | cut -d'-' -f1)
          echo "env_short=${ENV_SHORT}" >> $GITHUB_OUTPUT
          
          # Set ArgoCD server
          echo "server=argocd.${ENV}.wapps.com" >> $GITHUB_OUTPUT

      - name: Create editorial namespace
        run: |
          kubectl create namespace editorial --dry-run=client -o yaml | kubectl apply -f -

      # NOTE: Database credentials (editorial-db-credentials) are created by Ansible
      # during host provisioning. See: platform/host/mysql/install.yml

      - name: Create Strapi secrets
        run: |
          kubectl create secret generic editorial-strapi-secrets \
            --from-literal=APP_KEYS="${{ secrets.EDITORIAL_APP_KEYS }}" \
            --from-literal=API_TOKEN_SALT="${{ secrets.EDITORIAL_API_TOKEN_SALT }}" \
            --from-literal=ADMIN_JWT_SECRET="${{ secrets.EDITORIAL_ADMIN_JWT_SECRET }}" \
            --from-literal=TRANSFER_TOKEN_SALT="${{ secrets.EDITORIAL_TRANSFER_TOKEN_SALT }}" \
            --from-literal=JWT_SECRET="${{ secrets.EDITORIAL_JWT_SECRET }}" \
            --namespace=editorial \
            --dry-run=client -o yaml | kubectl apply -f -
          
          echo "✅ Strapi secrets created/updated"

      - name: Verify secrets exist
        run: |
          echo "Verifying editorial-strapi-secrets exists..."
          kubectl get secret editorial-strapi-secrets -n editorial
          
          echo "Verifying editorial-db-credentials exists..."
          kubectl get secret editorial-db-credentials -n editorial
          
          echo "✅ All required secrets verified"

      - name: Install ArgoCD CLI
        run: |
          if ! command -v argocd &> /dev/null; then
            curl -sSL -o argocd-linux-amd64 https://github.com/argoproj/argo-cd/releases/latest/download/argocd-linux-amd64
            chmod +x argocd-linux-amd64
            mkdir -p $HOME/.local/bin
            mv argocd-linux-amd64 $HOME/.local/bin/argocd
            echo "$HOME/.local/bin" >> $PATH
          fi

      - name: Login to ArgoCD
        run: |
          argocd login ${{ steps.env-vars.outputs.server }} \
            --username ${{ secrets.ARGO_INITIAL_USERNAME }} \
            --password ${{ secrets.ARGO_INITIAL_PASSWORD }} \
            --insecure --grpc-web

      - name: Trigger ArgoCD sync
        run: |
          echo "Triggering ArgoCD sync for editorial..."
          argocd app sync editorial --force --grpc-web
          
          echo "Waiting for sync to complete..."
          argocd app wait editorial --timeout 300 --grpc-web
          
          echo "✅ ArgoCD sync completed"

      - name: Restart deployment to pull latest image
        run: |
          echo "Restarting deployment to pull :latest image..."
          kubectl rollout restart deployment/editorial -n editorial
          
          echo "Waiting for rollout to complete..."
          kubectl rollout status deployment/editorial -n editorial --timeout=300s
          
          echo "✅ Deployment restarted successfully"

      - name: Apply environment configuration
        run: |
          ENV_SHORT="${{ steps.env-vars.outputs.env_short }}"
          
          # Apply editorial environment kustomization
          if [ -d "environments/${ENV_SHORT}/apps/editorial-kustomization" ]; then
            kubectl apply -k environments/${ENV_SHORT}/apps/editorial-kustomization/
            echo "✅ Environment configuration applied"
          fi

      - name: Apply network policies
        run: |
          ENV_SHORT="${{ steps.env-vars.outputs.env_short }}"
          
          if [ -f "environments/${ENV_SHORT}/platform/backend-network-policies.yaml" ]; then
            kubectl apply -f environments/${ENV_SHORT}/platform/backend-network-policies.yaml
            echo "✅ Network policies applied"
          fi

  summary:
    name: 'Deployment Summary'
    runs-on: ubuntu-latest
    needs: [build, deploy]
    if: always()
    steps:
      - name: Print summary
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "Editorial Service - Deployment Summary"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "Environment: ${{ needs.build.outputs.environment }}"
          echo "Image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest"
          echo ""
          echo "Job Results:"
          echo "  - Build: ${{ needs.build.result }}"
          echo "  - Deploy: ${{ needs.deploy.result }}"
          echo ""
          
          if [ "${{ needs.deploy.result }}" == "success" ]; then
            echo "✅ Editorial Service deployed successfully!"
            echo ""
            echo "Service endpoints:"
            echo "  - Admin Panel: https://editorial.${{ needs.build.outputs.environment }}.wapps.com/admin"
            echo "  - API: https://editorial.${{ needs.build.outputs.environment }}.wapps.com/api"
            echo "  - Health: http://editorial.editorial.svc.cluster.local:1337/_health"
            echo ""
            echo "Next steps:"
            echo "  1. Verify admin panel access"
            echo "  2. Check logs: kubectl logs -n editorial -l app=editorial --tail=50"
            echo "  3. Verify ingress: kubectl get ingress -n editorial"
          else
            echo "⚠️ Deployment completed with issues. Check logs above."
          fi
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
