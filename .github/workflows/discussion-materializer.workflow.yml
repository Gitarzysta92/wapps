name: 'Discussion Materializer - Build & Deploy'

on:
  push:
    branches: [develop, main]
    paths:
      - 'apps/services/discussion-materializer/**'
      - 'libs/infrastructure/**'
      - 'libs/cross-cutting/**'
      - 'apps/shared/**'
      - 'environments/dev/apps/discussion-materializer-kustomization/**'
      - 'argocd/applications/backend/discussion-materializer.yaml'
      - '.github/workflows/discussion-materializer.workflow.yml'
      - '.github/actions/sync-argocd-app/**'
      - '.github/actions/extract-namespace/**'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        default: 'development'
        type: choice
        options:
          - development
          - production

env:
  SERVICE_NAME: discussion-materializer
  REGISTRY: ghcr.io
  IMAGE_NAME: gitarzysta92/wapps/discussion-materializer
  NAMESPACE_MANIFEST: apps/services/discussion-materializer/provisioning/k8s/discussion.namespace.yaml

jobs:
  build:
    name: 'Build Docker Image'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      environment: ${{ steps.set-env.outputs.environment }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set environment
        id: set-env
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            ENV="${{ github.event.inputs.environment }}"
          elif [ "${{ github.ref }}" == "refs/heads/main" ]; then
            ENV="development"
          else
            ENV="development"
          fi
          echo "environment=${ENV}" >> $GITHUB_OUTPUT

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build application
        run: npx nx build apps.services.discussion-materializer --configuration=production

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: gitarzysta92
          password: ${{ secrets.CONTAINER_REGISTRY_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: dist/apps/services/discussion-materializer
          file: apps/services/discussion-materializer/Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          no-cache: true

  deploy:
    name: 'Deploy to ${{ needs.build.outputs.environment }}'
    runs-on: self-hosted
    needs: build
    environment: ${{ needs.build.outputs.environment }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set environment variables
        id: env-vars
        run: |
          ENV="${{ needs.build.outputs.environment }}"
          ENV_SHORT=$(echo "$ENV" | cut -d'-' -f1)
          echo "env_short=${ENV_SHORT}" >> $GITHUB_OUTPUT
          echo "server=argocd.${ENV}.wapps.com" >> $GITHUB_OUTPUT

          # MySQL connection (required via GitHub Environment vars)
          MYSQL_HOST="${{ vars.DISCUSSION_MYSQL_DATABASE_HOST }}"
          MYSQL_PORT="${{ vars.DISCUSSION_MYSQL_DATABASE_PORT }}"
          if [ -z "$MYSQL_HOST" ]; then
            echo "::error::Missing required GitHub Environment var DISCUSSION_MYSQL_DATABASE_HOST"
            exit 1
          fi
          if [ -z "$MYSQL_PORT" ]; then
            echo "::error::Missing required GitHub Environment var DISCUSSION_MYSQL_DATABASE_PORT"
            exit 1
          fi
          if ! [[ "$MYSQL_PORT" =~ ^[0-9]+$ ]] || [ "$MYSQL_PORT" -lt 1 ] || [ "$MYSQL_PORT" -gt 65535 ]; then
            echo "::error::Invalid MYSQL_PORT '$MYSQL_PORT'"
            exit 1
          fi
          echo "mysql_host=${MYSQL_HOST}" >> $GITHUB_OUTPUT
          echo "mysql_port=${MYSQL_PORT}" >> $GITHUB_OUTPUT

          # Discussion DB naming (required via GitHub Environment vars)
          DB_NAME="${{ vars.DISCUSSION_MYSQL_DATABASE_NAME }}"
          if [ -z "$DB_NAME" ]; then
            echo "::error::Missing required GitHub Environment var DISCUSSION_MYSQL_DATABASE_NAME"
            exit 1
          fi
          echo "db_name=${DB_NAME}" >> $GITHUB_OUTPUT

          DB_USERNAME="${{ vars.DISCUSSION_MYSQL_DATABASE_USERNAME }}"
          if [ -z "$DB_USERNAME" ]; then
            echo "::error::Missing required GitHub Environment var DISCUSSION_MYSQL_DATABASE_USERNAME"
            exit 1
          fi
          echo "db_username=${DB_USERNAME}" >> $GITHUB_OUTPUT

      - name: Extract namespace
        id: ns
        uses: ./.github/actions/extract-namespace
        with:
          manifest: ${{ env.NAMESPACE_MANIFEST }}

      - name: Create RabbitMQ credentials secret
        run: |
          kubectl apply -f ${{ env.NAMESPACE_MANIFEST }}
          kubectl create secret generic rabbitmq-credentials \
            --from-literal=username="${{ secrets.RABBITMQ_USERNAME }}" \
            --from-literal=password="${{ secrets.RABBITMQ_PASSWORD }}" \
            --namespace=${{ steps.ns.outputs.namespace }} \
            --dry-run=client -o yaml | kubectl apply --server-side --force-conflicts -f -

      - name: Create MinIO credentials secret
        run: |
          ACCESS_KEY="${{ secrets.MINIO_ROOT_USER }}"
          SECRET_KEY="${{ secrets.MINIO_ROOT_PASSWORD }}"
          [ -z "$ACCESS_KEY" ] && ACCESS_KEY="admin"
          [ -z "$SECRET_KEY" ] && SECRET_KEY="changeme"
          kubectl apply -f ${{ env.NAMESPACE_MANIFEST }}
          kubectl create secret generic minio-credentials \
            --from-literal=accessKey="$ACCESS_KEY" \
            --from-literal=secretKey="$SECRET_KEY" \
            --namespace=${{ steps.ns.outputs.namespace }} \
            --dry-run=client -o yaml | kubectl apply --server-side --force-conflicts -f -

      - name: Fail fast if MySQL secrets missing (discussion)
        run: |
          for var in MYSQL_DATABASE_USERNAME MYSQL_DATABASE_PASSWORD DISCUSSION_MYSQL_DATABASE_PASSWORD; do
            if [ -z "${!var}" ]; then
              echo "::error::Missing required GitHub Environment secret $var"
              exit 1
            fi
          done
        env:
          MYSQL_DATABASE_USERNAME: ${{ vars.MYSQL_DATABASE_USERNAME }}
          MYSQL_DATABASE_PASSWORD: ${{ secrets.MYSQL_DATABASE_PASSWORD }}
          DISCUSSION_MYSQL_DATABASE_PASSWORD: ${{ secrets.DISCUSSION_MYSQL_DATABASE_PASSWORD }}

      - name: Bootstrap MySQL database + user (discussion)
        uses: ./.github/actions/bootstrap-mysql-db
        with:
          mysql-host: ${{ steps.env-vars.outputs.mysql_host }}
          mysql-port: ${{ steps.env-vars.outputs.mysql_port }}
          mysql-admin-username: ${{ vars.MYSQL_DATABASE_USERNAME }}
          mysql-admin-password: ${{ secrets.MYSQL_DATABASE_PASSWORD }}
          database-name: ${{ steps.env-vars.outputs.db_name }}
          app-username: ${{ steps.env-vars.outputs.db_username }}
          app-password: ${{ secrets.DISCUSSION_MYSQL_DATABASE_PASSWORD }}

      - name: "Create MySQL credentials secret (shared: discussion-app-secrets)"
        run: |
          kubectl apply -f ${{ env.NAMESPACE_MANIFEST }}
          kubectl create secret generic discussion-app-secrets \
            --from-literal=DISCUSSION_DATABASE_HOST="${{ steps.env-vars.outputs.mysql_host }}" \
            --from-literal=DISCUSSION_DATABASE_PORT="${{ steps.env-vars.outputs.mysql_port }}" \
            --from-literal=DISCUSSION_DATABASE_USERNAME="${{ steps.env-vars.outputs.db_username }}" \
            --from-literal=DISCUSSION_DATABASE_PASSWORD="${{ secrets.DISCUSSION_MYSQL_DATABASE_PASSWORD }}" \
            --from-literal=DISCUSSION_DATABASE_NAME="${{ steps.env-vars.outputs.db_name }}" \
            --namespace=${{ steps.ns.outputs.namespace }} \
            --dry-run=client -o yaml | kubectl apply --server-side --force-conflicts -f -

      - name: Create MongoDB credentials secret (materializer)
        run: |
          kubectl apply -f ${{ env.NAMESPACE_MANIFEST }}
          kubectl create secret generic discussion-materializer-mongo \
            --from-literal=username="${{ secrets.MONGODB_WAPPS_USERNAME }}" \
            --from-literal=password="${{ secrets.MONGODB_WAPPS_PASSWORD }}" \
            --namespace=${{ steps.ns.outputs.namespace }} \
            --dry-run=client -o yaml | kubectl apply --server-side --force-conflicts -f -

      - name: Create GHCR image pull secret
        run: |
          kubectl apply -f ${{ env.NAMESPACE_MANIFEST }}
          kubectl create secret docker-registry ghcr-secret \
            --docker-server=ghcr.io \
            --docker-username=gitarzysta92 \
            --docker-password="${{ secrets.CONTAINER_REGISTRY_TOKEN }}" \
            --namespace=${{ steps.ns.outputs.namespace }} \
            --dry-run=client -o yaml | kubectl apply --server-side --force-conflicts -f -

      - name: Sync ArgoCD Application
        uses: ./.github/actions/sync-argocd-app
        with:
          app-name: ${{ env.SERVICE_NAME }}
          argocd-server: ${{ steps.env-vars.outputs.server }}
          argocd-username: ${{ secrets.ARGO_INITIAL_USERNAME }}
          argocd-password: ${{ secrets.ARGO_INITIAL_PASSWORD }}
          app-manifest-path: argocd/applications/backend/discussion-materializer.yaml
          sync-timeout: '300'
          force-sync: 'true'

      - name: Restart deployment to pull latest image
        run: |
          kubectl rollout restart deployment/discussion-materializer -n ${{ steps.ns.outputs.namespace }}
          kubectl rollout status deployment/discussion-materializer -n ${{ steps.ns.outputs.namespace }} --timeout=600s

      - name: Apply environment configuration
        run: |
          ENV_SHORT="${{ steps.env-vars.outputs.env_short }}"
          if [ -d "environments/${ENV_SHORT}/apps/discussion-materializer-kustomization" ]; then
            kubectl apply -k environments/${ENV_SHORT}/apps/discussion-materializer-kustomization/
          fi

