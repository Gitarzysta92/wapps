{
  "name": "apps.services.authenticator",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/services/authenticator/src",
  "projectType": "application",
  "tags": ["type:app", "scope:platform", "layer:auth"],
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/services/authenticator",
        "main": "apps/services/authenticator/src/main.ts",
        "tsConfig": "apps/services/authenticator/tsconfig.app.json",
        "webpackConfig": "apps/services/authenticator/webpack.config.js"
      },
      "configurations": {
        "development": {
          "optimization": false,
          "sourceMap": true
        },
        "production": {
          "optimization": true,
          "sourceMap": false
        }
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "apps.services.authenticator:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "apps.services.authenticator:build:development"
        },
        "production": {
          "buildTarget": "apps.services.authenticator:build:production"
        }
      }
    }
  }
}

