{
  "name": "apps.services.discussion",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/services/discussion/src",
  "projectType": "application",
  "tags": ["type:app", "scope:discussion"],
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/services/discussion",
        "main": "apps/services/discussion/src/main.ts",
        "tsConfig": "apps/services/discussion/tsconfig.app.json",
        "webpackConfig": "apps/services/discussion/webpack.config.js"
      },
      "configurations": {
        "development": {
          "optimization": false,
          "sourceMap": true
        },
        "production": {
          "optimization": true,
          "sourceMap": false
        }
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "apps.services.discussion:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "apps.services.discussion:build:development"
        },
        "production": {
          "buildTarget": "apps.services.discussion:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/services/discussion/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
