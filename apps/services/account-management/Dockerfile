FROM node:20-alpine

WORKDIR /app

# Copy the built application from NX
COPY dist/apps/services/account-management .

# Install production dependencies (generated by NX)
RUN npm install --omit=dev

# Run as non-root user
USER node

EXPOSE 1340

CMD ["node", "main.js"]

