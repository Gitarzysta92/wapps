FROM node:20-alpine

WORKDIR /app

# Copy the built application from NX
COPY dist/apps/services/firebase-auth-validator .

# Install production dependencies (generated by NX)
RUN npm install --omit=dev

# Run as non-root user
USER node

EXPOSE 8080

CMD ["node", "main.js"]
