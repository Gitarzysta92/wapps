{
  "name": "portals-shared-cross-cutting-navigation",
  "$schema": "../../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "prefix": "id",
  "sourceRoot": "apps/portals/shared/cross-cutting/navigation",
  "tags": ["type:cross-cutting", "scope:navigation"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/portals/shared/cross-cutting/navigation",
        "main": "apps/portals/shared/cross-cutting/navigation/src/index.ts",
        "tsConfig": "apps/portals/shared/cross-cutting/navigation/tsconfig.lib.json",
        "assets": ["apps/portals/shared/cross-cutting/navigation/*.md"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/portals/shared/cross-cutting/navigation/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    }
  }
}
