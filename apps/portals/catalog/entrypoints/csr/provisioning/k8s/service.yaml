apiVersion: v1
kind: Service
metadata:
  name: catalog-csr-service
  labels:
    app: catalog-csr
    app.kubernetes.io/name: catalog-csr
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: catalog-csr
