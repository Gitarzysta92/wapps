apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-csr
  labels:
    app: catalog-csr
    app.kubernetes.io/name: catalog-csr
    app.kubernetes.io/component: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: catalog-csr
  template:
    metadata:
      labels:
        app: catalog-csr
        app.kubernetes.io/name: catalog-csr
        app.kubernetes.io/component: frontend
    spec:
      imagePullSecrets:
      - name: ghcr-secret
      containers:
      - name: catalog-csr
        image: ghcr.io/gitarzysta92/wapps/catalog-csr:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
          name: http
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "80"
        resources:
          limits:
            cpu: 200m
            memory: 256Mi
          requests:
            cpu: 100m
            memory: 128Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
