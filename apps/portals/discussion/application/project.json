{
  "name": "discussion-angular",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "prefix": "discussion",
  "sourceRoot": "apps/portals/discussion/application/src",
  "tags": ["type:lib"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/portals/discussion/application/jest.config.ts",
        "passWithNoTests": true
      }
    }
  }
}

