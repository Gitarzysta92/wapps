apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aggregator-ssg-ingress
  namespace: aggregator
  labels:
    app: aggregator-ssg
    app.kubernetes.io/name: aggregator-ssg
    app.kubernetes.io/component: frontend
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - host: aggregator-ssg.wapps.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: aggregator-ssg-service
            port:
              number: 80
