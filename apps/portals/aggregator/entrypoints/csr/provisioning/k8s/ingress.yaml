apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aggregator-csr-ingress
  labels:
    app: aggregator-csr
    app.kubernetes.io/name: aggregator-csr
    app.kubernetes.io/component: frontend
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: aggregator-csr.host.placeholder  # This will be overridden by environment overlays
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: aggregator-csr-service
            port:
              number: 80
