apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: management-portal-csr-ingress
  labels:
    app: management-portal-csr
    app.kubernetes.io/name: management-portal-csr
    app.kubernetes.io/component: frontend
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: management-portal-csr.host.placeholder  # overridden by environment overlays
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: management-portal-csr-service
            port:
              number: 80

