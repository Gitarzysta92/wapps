apiVersion: v1
kind: Service
metadata:
  name: aggregator-demo-csr-service
  labels:
    app: aggregator-demo-csr
    app.kubernetes.io/name: aggregator-demo-csr
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: aggregator-demo-csr

