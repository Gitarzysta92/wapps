apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aggregator-demo-csr-ingress
  namespace: aggregator
  labels:
    app: aggregator-demo-csr
    app.kubernetes.io/name: aggregator-demo-csr
    app.kubernetes.io/component: frontend
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: aggregator-demo-csr.host.placeholder  # This will be overridden by environment overlays
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: aggregator-demo-csr-service
            port:
              number: 80

