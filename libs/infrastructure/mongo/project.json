{
  "name": "mongo",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/infrastructure/mongo/src",
  "projectType": "library",
  "tags": ["type:infra", "scope:infrastructure"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/../../../dist/libs/infrastructure/mongo"],
      "options": {
        "command": "tsc --build libs/infrastructure/mongo/tsconfig.lib.json"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/infrastructure/mongo/jest.config.ts"
      }
    }
  }
}
