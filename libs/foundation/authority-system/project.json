{
  "name": "authority-system",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/foundation/authority-system/src",
  "tags": [
    "type:foundation",
    "scope:authority-system"
  ],
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "libs/foundation/authority-system/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ]
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/../../../dist/foundation/authority-system"
      ],
      "options": {
        "command": "tsc --build libs/foundation/authority-system/tsconfig.lib.json"
      }
    }
  }
}
