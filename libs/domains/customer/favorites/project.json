{
  "name": "domains-customer-favorites",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "prefix": "domains",
  "sourceRoot": "libs/domains/customer/favorites/src",
  "tags": [
    "type:domain",
    "scope:customer"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/../../dist/domains/customer/favorites"
      ],
      "options": {
        "command": "tsc --build libs/domains/customer/favorites/tsconfig.lib.json"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ]
    }
  }
}
