{
  "name": "catalog-versioning",
  "$schema": "../../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "prefix": "cat",
  "sourceRoot": "libs/domains/catalog/versioning/src",
  "tags": [
    "type:domain",
    "scope:catalog"
  ],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ]
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/../../dist/domains/catalog/versioning"
      ],
      "options": {
        "command": "tsc --build libs/domains/catalog/versioning/tsconfig.lib.json"
      }
    }
  }
}
