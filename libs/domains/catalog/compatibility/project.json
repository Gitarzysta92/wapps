{
  "name": "catalog-compatibility",
  "$schema": "../../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "prefix": "cat",
  "sourceRoot": "libs/domains/catalog/compatibility/src",
  "tags": [
    "type:domain",
    "scope:catalog"
  ],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ]
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/../../dist/domains/catalog/compatibility"
      ],
      "options": {
        "command": "tsc --build libs/domains/catalog/compatibility/tsconfig.lib.json"
      }
    }
  }
}
