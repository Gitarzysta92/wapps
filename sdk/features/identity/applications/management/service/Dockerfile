FROM node:20-alpine

WORKDIR /app

# Build context should be the prebuilt dist output directory:
# dist/apps/services/account-management
COPY . ./

# Install production dependencies (generated by NX)
RUN npm install --omit=dev

# Run as non-root user
USER node

EXPOSE 1340

CMD ["node", "main.js"]

