export enum ContentNodeVisibility {
  PUBLIC = 'public',
  UNLISTED = 'unlisted',
  PRIVATE = 'private',
}

export enum ContentNodeState {
  DRAFT = 'draft',
  PUBLISHED = 'published',
  ARCHIVED = 'archived',
}


export enum ContentNodeRelationType {
  ASSOCIATED = 'associated',
  BELONGS_TO = 'belongs_to',
}
