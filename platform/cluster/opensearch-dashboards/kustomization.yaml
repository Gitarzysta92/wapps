apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: opensearch

# Note: OpenSearch Dashboards is managed by Helm chart via ArgoCD
# This directory only contains values.yaml for Helm chart configuration
# No Kubernetes resources to manage directly here
