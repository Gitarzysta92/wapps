{#-
  Environment variables required by acme.sh DNS provider plugins.
  Values are provided via `acme_dns_env` (dict).

  NOTE: Keep this file mode 0600; it may contain secrets.
-#}
{% for k, v in (acme_dns_env | default({})).items() %}
{{ k }}={{ v }}
{% endfor %}

