---
- name: Install dnsmasq
  package:
    name: dnsmasq
    state: present

# We don't need dnsmasq.d directory since we're using main config file only

- name: Create main dnsmasq configuration
  template:
    src: dnsmasq-main.conf.j2
    dest: /etc/dnsmasq.conf
    mode: '0644'

- name: Reload systemd daemon
  systemd:
    daemon_reload: yes

- name: Start and enable dnsmasq
  systemd:
    name: dnsmasq
    state: started
    enabled: yes

- name: Restart systemd-resolved
  systemd:
    name: systemd-resolved
    state: restarted

- name: Wait for dnsmasq to start
  wait_for:
    port: 5354
    host: "127.0.0.1"
    timeout: 10
  ignore_errors: yes

- name: Test DNS resolution through systemd-resolved
  shell: |
    nslookup argocd.{{ target_env }}.wapps.ai 127.0.0.53
  register: dns_test_result
  changed_when: false
  failed_when: false
  when: target_ip is defined and target_ip != ""

- name: Debug DNS test result
  debug:
    msg: |
      DNS Test Result for {{ target_env }}.wapps.ai:
      {{ dns_test_result.stdout | default('DNS test not run') }}
  when: dns_test_result is defined

- name: Display DNS configuration status
  debug:
    msg: |
      {% if target_ip is defined and target_ip != "" %}
      ✅ DNS configuration completed successfully!
      
      🌐 Wildcard DNS: *.{{ target_env }}.wapps.ai → {{ target_ip }}
      
      📍 Service URLs (all resolve to the same IP):
      - ArgoCD: https://argocd.{{ target_env }}.wapps.ai
      - K3s API: https://k3s-api.{{ target_env }}.wapps.ai
      - Node Exporter: http://metrics.{{ target_env }}.wapps.ai
      - Grafana: https://grafana.{{ target_env }}.wapps.ai
      - Prometheus: https://prometheus.{{ target_env }}.wapps.ai
      - ANY service: https://anything.{{ target_env }}.wapps.ai
      
      🔧 DNS Server info:
      - systemd-resolved: 127.0.0.53:53 (stub listener)
      - dnsmasq: 127.0.0.1:5353 (upstream)
      - Environment: {{ target_env }}
      - Target IP: {{ target_ip }}
      
      📋 How it works:
      1. Apps query 127.0.0.53:53 (systemd-resolved)
      2. systemd-resolved forwards to 127.0.0.1:5353 (dnsmasq)
      3. dnsmasq resolves {{ target_env }}.wapps.ai domains to {{ target_ip }}
      4. Other domains are forwarded to 1.1.1.1 and 9.9.9.9
      {% else %}
      ❌ DNS configuration failed - missing target_ip variable
      {% endif %}
